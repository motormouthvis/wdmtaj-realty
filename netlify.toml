# Popup tag is production: snippets/explorer-tag.html
# (https://app.dreamneighborhood.com/explorer/sdk.js).
# Pages already carrying the tag are left unchanged.
[build]
  command = "node tools/inject-explorer-tag.mjs"
  publish = "."
